function handleCallbackQuery(bot, query) {
    const data = query.data;

    if (data === 'option1') {
        // Handle Option 1 button press
    } else if (data === 'option2') {
        // Handle Option 2 button press
    }
}

module.exports = {
    handleCallbackQuery,
};
